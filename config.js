// Will load offlineFile (specified below) from disk instead of trying to fetch it from the internet
const offline = true;
const offlineFile = "sample/slow_caption.xml";
const lineDisplayDuration = 4; // [in s]
const maxLinesToDisplay = -1; // -1 for not restriction
const fps = 10;
const hideCaptionBox = false;
